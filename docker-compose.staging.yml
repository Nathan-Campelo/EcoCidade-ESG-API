services:
  app:
    container_name: projeto-esg-app-staging
    environment:
      APP_ENVIRONMENT: staging
    ports:
      - "${APP_PORT:-8081}:8080"

  db:
    container_name: projeto-esg-db-staging
    ports:
      - "${DB_PORT:-5433}:5432"
