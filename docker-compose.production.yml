services:
  app:
    container_name: projeto-esg-app-production
    environment:
      APP_ENVIRONMENT: production
    ports:
      - "${APP_PORT:-8082}:8080"

  db:
    container_name: projeto-esg-db-production
    ports:
      - "${DB_PORT:-5434}:5432"
